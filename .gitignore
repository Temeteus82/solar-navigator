/target
/dist
/vendor/cspice

# Written into the repo root by `./scripts/sweep.sh stamp` (cargo-sweep) to mark
# the current artefacts as in-use. Local state, never committed.
/sweep.timestamp

# Locally generated GPU-compressed textures (platform-specific block format:
# BC7 on desktop, ASTC on Apple Silicon). Regenerate with scripts/compress_textures.*
assets/textures/*.dds
assets/textures/*.ktx2

.DS_Store
**/.DS_Store
._*
**/._*
.env
.env.*
dist-test/

# Claude Code local harness state (scheduled task locks, agent worktrees)
/.claude/
